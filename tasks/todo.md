# Elevator Unity Setup Tasks

- [x] Project folders created under `Assets`/`Packages`/`ProjectSettings`.
- [x] `InputSystem` dependency registered and active input handler set to Input System.
- [x] Portrait-first player settings baseline applied (540x960 UI baseline, orientation lock).
- [x] 2D layer and sorting setup added (`BG`, `Character`, `FX`, `GasFX`, `UI`).
- [x] Scene bootstrap script added (`Tools/Elevator/Create Main Scene`, `Tools/Elevator/Validate Main Scene`).
- [x] Runtime skeleton scene cleaned of sample content.
- [x] Config ScriptableObject asset scaffold created (`Assets/Resources/ElevatorProjectConfig.asset`).
- [x] Input map includes Hold/AnyTap + Space/Mobile/Mouse bindings for setup.
- [ ] UI prefab/overlay and placeholder root prefab set for future gameplay UI.
- [ ] Build and platform validation for Android/iOS/WebGL executed.

# Notes
- Current Unity project is in setup state; runtime gameplay logic is intentionally not attached yet.
